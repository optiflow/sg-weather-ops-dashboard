pre-commit:
  parallel: false
  commands:
    lint:
      run: npm run lint
    test:
      run: npm test

pre-push:
  parallel: false
  commands:
    build:
      run: npm run build
    e2e:
      run: npm run test:e2e
